import * as React from "react";
import {connect} from "react-redux";

const Cues:React.FC = () => {
    return (
        <div>
            <h2>
                CUES
            </h2>
        </div>
    )
}

export default connect(null, null)(Cues)